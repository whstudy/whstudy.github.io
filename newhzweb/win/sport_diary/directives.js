define(['angular'],function (angular) {
'use strict';

angular.module('winWeb.directives.sport_diary', []).directive('', function() {
	return function(scope, element, attrs) {
		//$(element).freeSlide();
	};
});
});